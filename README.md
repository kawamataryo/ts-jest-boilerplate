# ts-jest boilerplate

A boilerplate for TypeScript + Jest.

# Usage

## Build
Compiled 'src/' files to 'dist/'.

```bash
$ yarn build
```

## Test
Run jest.

```bash
$ yarn test
```

## Lint
Run eslint.

```bash
$ yarn lint --fix
```